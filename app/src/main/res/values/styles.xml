<resources>

    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme" parent="BaseTheme">
        <!-- Customize your theme here. -->
    </style>

    <style name="SplashScreen" parent="Base.Theme.AppCompat.Dialog.FixedSize">
             <item name="android:padding">0dp</item>
             <item name="android:windowFrame">@null</item>
           <!--<item name="android:windowBackground">@mipmap/ic_launcher</item>-->
              <item name="android:windowNoTitle">true</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- 水平分割线 -->
    <style name="horizonal_halving_line">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">2px</item>
        <item name="android:background">#e7eaea</item>
    </style>
    <!-- 垂直分割线 -->
    <style name="vertical_halving_line">
        <item name="android:layout_width">2dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">#e7eaea</item>
    </style>

    <style name="BaseText" >
        <item name="android:textColor">#fff</item>

    </style>

    <style name="DetailTitleText" parent="BaseText">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="DetailValueText" parent="BaseText">
        <item name="android:textSize">14sp</item>

    </style>



</resources>
